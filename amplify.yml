version: 1
frontend:
  phases:
    preBuild:
      commands:
        - npm ci --include=optional --cache .npm
    build:
      commands:
        - npm run build
  artifacts:
    baseDirectory: dist
    files:
      - "**/*"
  cache:
    paths:
      - .npm/**/*
